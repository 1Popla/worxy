<div class="container mx-auto p-4">
  <div class="bg-white shadow rounded-lg p-4">
    <h2 class="text-lg font-bold text-gray-800 mb-4">Szczegóły Powiadomienia</h2>
    <div class="mb-4">
      <p class="text-gray-600"><strong>From:</strong> <%= link_to @notification.actor.email, user_path(@notification.actor), class: 'text-blue-600 hover:text-blue-900' %></p>
      <p class="text-gray-600"><strong>Regarding:</strong> <%= link_to @notification.notifiable.title, post_path(@notification.notifiable), class: 'text-blue-600 hover:text-blue-900' %></p>
      <p class="text-gray-600"><strong>Action:</strong> <%= @notification.action %></p>
      <p class="text-gray-600"><strong>Sent at:</strong> <%= @notification.created_at.strftime("%b %d, %Y %H:%M") %></p>
    </div>
    <div class="mb-4">
      <h3 class="text-md font-bold text-gray-800 mb-2">Post Details</h3>
      <p class="text-gray-600"><strong>Title:</strong> <%= @notification.notifiable.title %></p>
      <p class="text-gray-600"><strong>Description:</strong> <%= @notification.notifiable.description %></p>
      <p class="text-gray-600"><strong>Price:</strong> <%= number_to_currency(@notification.notifiable.price) %></p>
    </div>
    <div class="mb-4">
      <h3 class="text-md font-bold text-gray-800 mb-2">Worker Details</h3>
      <p class="text-gray-600"><strong>Name:</strong> <%= @notification.actor.first_name %></p>
      <p class="text-gray-600"><strong>Email:</strong> <%= @notification.actor.email %></p>
      <p class="text-gray-600"><strong>Profile:</strong> <%= link_to "View Profile", user_path(@notification.actor), class: 'text-blue-600 hover:text-blue-900' %></p>
    </div>
  </div>
</div>
