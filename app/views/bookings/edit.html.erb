  <div class="flex-1 overflow-auto">
    <div class="container mx-auto px-4 py-8">
      <div class="bg-white shadow-lg rounded-lg p-8 relative">
        <h1 class="text-3xl font-semibold mb-6 border-b pb-4">Edytuj Rezerwację</h1>
        
        <%= render 'form', booking: @booking %>

        <div class="mt-8 flex space-x-4">
          <%= link_to 'Wróć', bookings_path, class: 'bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded' %>
        </div>
      </div>
    </div>
  </div>
