<div class="flex h-screen bg-gray-100 overflow-hidden">
  <%= render 'shared/dashboard/sidebar' %>

  <div class="flex-1 overflow-auto">
    <div class="container mx-auto px-4 py-8">
      <h1 class="text-3xl font-bold mb-4">My Posts</h1>
      <% if @user_posts.present? %>
        <div class="flex flex-col space-y-4">
          <% @user_posts.order(created_at: :desc).each do |post| %>
            <div class="w-full">
              <%= render partial: 'posts/post_card', locals: { post: post } %>
            </div>
          <% end %>
        </div>
        <div class="mt-6">
          <%= paginate @user_posts %>
        </div>
      <% else %>
        <div class="w-full text-center">
          <p>No posts available. Why not create one?</p>
          <%= link_to 'Add New Post', new_post_path, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
