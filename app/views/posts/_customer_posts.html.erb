<div class="flex flex-col space-y-4">
  <% @customer_posts.order(created_at: :desc).each do |post| %>
    <div class="w-full">
      <%= render partial: 'posts/post_card', locals: { post: post } %>
    </div>
  <% end %>
</div>
